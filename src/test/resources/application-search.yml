spring:
  application:
    name: 1build-ui

server:
  port: 0

onebuild:
  ui:
    main-component: search
    test:
      css:
        - path: /app/css
          source-path: /app/css
          resources:
            - search.css
      components:
        resource-type: COMPONENT
        path: /app/components
        source-path: /app/modules
        list:
          - name: search
            home: /search
