spring:
  application:
    name: 1build-ui

server:
  port: 0

onebuild:
  ui:
    index:
      main-component: datasource

    component:
      path: /app/components
      source-path: /app/modules
      list:
        datasource:
          home: /datasource