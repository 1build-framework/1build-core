onebuild:
  ui:
    component:
      enabled: true
      path: /app/components
      source-path: /app/modules
      list:
        app:
          home: /app
        about:
          home: /about
    index:
      main-component: about