onebuild:
  ui:
    main-component: about
    test:
      components:
        resource-type: COMPONENT
        path: /app/components
        source-path: /app/modules
        list:
          - name: app
            home: /app
          - name: about
            home: /about